{
  "name": "zxspectrum",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npm run build && node --experimental-wasm-modules test/test.js test/tests.in test/tests.expected",
    "build:core": "node generator/gencore.js generator/core.ts.in build/core.ts",
    "build:wasm:debug": "asc build/core.ts --target debug",
    "build:wasm:release": "asc build/core.ts --target release",
    "build:js": "mkdir -p dist/jsspeccy && cross-env NODE_OPTIONS=--openssl-legacy-provider webpack",
    "build:public": "mkdir -p dist/jsspeccy && cp public/index.html dist/ && cp public/favicon.ico dist/ && cp README.md dist/ && cp COPYING dist/ && cp CHANGELOG.md dist/ && cp -r public/roms dist/jsspeccy && cp -r public/tapeloaders dist/jsspeccy && cp -r public/canvasgui dist && cp -r public/img dist",
    "build": "npm run build:core && npm run build:wasm:debug && npm run build:js && npm run build:public",
    "build:release": "npm run build:core && npm run build:wasm:release && npm run build:js && npm run build:public",
    "watch": "npm-watch"
  },
  "dependencies": {
    "file-dialog": "^0.0.8",
    "jszip": "^3.7.1",
    "pako": "^2.0.4"
  },
  "devDependencies": {
    "assemblyscript": "^0.19.6",
    "cross-env": "^7.0.3",
    "npm-watch": "^0.10.0",
    "svg-inline-loader": "^0.8.2",
    "webpack": "^5.44.0",
    "webpack-cli": "^4.7.2"
  },
  "watch": {
    "build:core": {
      "patterns": [
        "generator/*.js",
        "generator/*.ts.in"
      ],
      "extensions": [
        "js",
        "in"
      ]
    },
    "build:wasm:debug": {
      "patterns": [
        "build/core.ts"
      ],
      "extensions": "ts"
    },
    "build:js": {
      "patterns": [
        "runtime/*.js",
        "runtime/icons/*.svg",
        "build/*.js"
      ],
      "extensions": [
        "js",
        "svg"
      ]
    },
    "build:public": {
      "patterns": [
        "public/*.html",
        "public/*.rom"
      ],
      "extensions": [
        "html",
        "rom"
      ]
    }
  },
  "jest": {
    "testEnvironment": "jsdom",
    "transform": {
      "^.+\\.[t|j]sx?$": "babel-jest",
      ".+\\.(css|styl|less|sass|scss|svg|png|jpg|ttf|woff|woff2)(\\?inline)?$": "jest-transform-stub"
    },
    "globals": {
      "STAGING_ENV": "prod"
    }
  },
  "babel": {
    "presets": [
      "@babel/preset-env",
      "@babel/preset-react"
    ],
    "plugins": [
      "@babel/plugin-transform-runtime"
    ]
  }
}
